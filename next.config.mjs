/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/travel-china-with-xuan',
  trailingSlash: true,
  images: { unoptimized: true },
};

export default nextConfig;
