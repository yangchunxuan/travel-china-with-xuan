# Ten-city stay network QA report

Run date: 2026-08-20 (Asia/Shanghai)

Baseline: `origin/main` `ef189874`

This is a scoped content/contract QA record. It is not a production build or a
deployment certificate.

## Passing checks

- `git diff --check` on the project changes.
- `node --experimental-strip-types --test` for:
  - `supabase/tests/stay-network-contract.test.mjs`;
  - `supabase/tests/destination-hub-static.test.mjs`;
  - `supabase/tests/page-family-body.test.mjs`;
  - `supabase/tests/seo-owner-strengthening-static.test.mjs`.
  Final scoped result: **30/30 tests passed**.
- Contract checks cover exact ten-city identity, public forbidden fields,
  deny-by-default supplier sharing, three-layer foreign-guest evidence,
  request-scoped quotes and restricted-data bundle isolation.
- Network integration checks cover one row per city, EN/ZH/KO block parity,
  four nationwide owner links, one human stay handoff, localized paths and the
  complete-versus-partial source-date boundary.
- Scoped TypeScript `tsc --noEmit` on the changed body files, both shared Hub
  builders and `lib/stayNetworkContract.ts`, using the repository's installed
  dependency set without generating files.
- Changed metadata parses as JSON; source-review and date-modified invariants
  are separately asserted.

## Expected integration checks not passed in this branch

`npm run guide:check` reports the generated guide registry missing/stale, and
`npm run content:check` reports the generated content manifest stale. This is
expected after metadata updates because the task explicitly forbids modifying
the central Registry/generated integration baseline in the article branch.
Central must regenerate and review those artifacts during integration.

A full repository `tsc --noEmit` cannot complete in this isolated worktree
because the ignored generated guide body/registry modules are absent. A scoped
TypeScript check of every changed TypeScript source passes. No statement in
this package says “build passed”.

## Editorial/source checks

- Five established Hubs and five city stay owners preserve three-language
  block order/type and localized internal links.
- Hangzhou and Zhangjiajie shared builders generate the same block structure
  for all three locales and contain each nationwide stay owner once per locale.
- `foreigners-china-hotel` aligns to the complete 2026-08-20 official-source
  reopen record.
- `china-accessible-hotel-room-verification` updates five factual official
  source items and metadata to 2026-08-20 while retaining the image-credit date.
- `china-last-night-before-international-flight` retains its 2026-08-11 factual
  review date because the 2026-08-20 pass was partial; the new source log names
  what failed to reopen.
- Hub factual review dates do not move merely because decision copy and links
  changed.

## Image/privacy checks

- No AI hotel, room, street, accessibility or guest-documentary image was
  created or added.
- Changed live-baseline pages keep real repository photographs; their observable
  claim boundary and remaining rights/scene gaps are recorded in
  `image-source-qa.md`.
- No passport, order, payment, QR, room/key-card, guest or staff identity data is
  present in new copy or assets.
- Guilin and the unreleased Chongqing/Shenzhen Hub packages remain asset/release
  gaps, not synthetic-image substitutions.

## Release state

- Branch only; no PR, merge or deployment.
- Hangzhou and Zhangjiajie are registered in the audited main runtime, but this
  QA does not claim their public deployment was verified.
- Structured stay inquiry, supplier consent and private property/quote storage
  remain an atomic central implementation gap.

Final package status:

**STAY AND HOTEL CONVERSION NETWORK READY — CENTRAL REVIEW REQUIRED**
