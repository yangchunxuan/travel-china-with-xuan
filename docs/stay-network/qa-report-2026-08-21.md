# Stay-network integration-split QA report

Run date: 2026-08-21 (Asia/Shanghai)

Baseline: `origin/main` `ef189874` (PR #74 merged and published)

Branch: `article/worker-5-stay-network-integration-split-20260821`

This file is a branch verification record, not a deployment certificate.
Results are appended only after the corresponding command or browser check has
actually run.

## CTA privacy-boundary repair

The follow-up repair changes the 38 public EN/ZH/KO Hub and stay-owner body
files that carried the accommodation handoff. Every affected CTA now treats the
existing general planner as a first-contact channel only. Initial submission is
limited to travel dates, city/area, traveller and room counts, an optional rough
budget, and a request to discuss family or accessibility arrangements later.

The copy expressly says not to submit children's specific ages, mobility,
medical or accessibility details, passport/document information, booking/order
or payment records, or identifiable material intended for a supplier. A later
human conversation handles necessary detail only with suitable consent, and
supplier sharing requires separate, purpose-specific consent. This repair adds
no `StayHandoff`, supplier connection, inventory, payment flow, storage or
analytics wiring.

Post-repair command results are recorded below only after execution.

| Post-repair check | Result |
| --- | --- |
| Privacy/public-content, contract, Hub, page-family and owner tests | Passed: 28/28. The privacy test reads all 38 affected public body files in EN/ZH/KO and rejects the superseded direct-submission language. |
| `npm run guide:generate` and `npm run guide:check` | Passed: 157 independent guide folders verified. |
| `npm run content:generate` and `npm run content:check` | Passed: zero manifest entries generated and verified from the one current input. |
| `npm run typecheck` | Passed. |
| `npm run build` | Passed: compile, type/data checks, 694-page static generation, production export pruning, font coverage and postbuild search-platform export completed. |
| `npm run check:indexable-export` | Passed: 12 priority index targets. |
| `npm run check:search-platform-export` | Passed: 27 section Hubs, 81 collection Hubs and three Evan profiles; all indexable internal `href`/`src` targets resolved. |
| `git diff --check` | Passed after the final QA edit. |

The 15-route browser sample below was not rerun for this copy-only repair. No
block IDs, block types, links, templates or layout code changed; the production
build and static exports were regenerated successfully. The earlier browser
record remains identified as the pre-repair sample rather than being
misrepresented as a new run.

## Required command checks

| Check | Command | Result |
| --- | --- | --- |
| Guide generation | `npm run guide:generate` | Passed: 157 independent guide folders verified; two temporary generated files refreshed. |
| Content generation | `npm run content:generate` | Passed: zero manifest entries generated from the one current content input. |
| Generated guide check | `npm run guide:check` | Passed. |
| Generated content check | `npm run content:check` | Passed: zero generated entries verified. |
| Full inquiry suite | `npm run test:inquiry` | Ran in full: 326/329 passed and three unchanged baseline checks failed. See the bounded failure record below. |
| TypeScript | `npm run typecheck` | Passed. |
| Production build and postbuild export checks | `npm run build` | Passed: compile, type/data checks, 694-page static export, postbuild prune, font coverage and search-platform export completed. |
| Indexable export | `npm run check:indexable-export` | Passed: 12 priority targets. |
| Search-platform export | `npm run check:search-platform-export` | Passed: 27 section Hubs, 81 collection Hubs and three Evan profiles; internal `href`/`src` targets resolved. |
| Whitespace/conflict check | `git diff --check ef189874...HEAD` and working-tree `git diff --check` | Passed after the final QA edit. |

### Full-inquiry bounded failures

The full suite ran 329 tests: 326 passed and three failed. None touches a file
changed by this branch:

- `older-parents guide is registered as a localized planning guide` relies on
  a fixed neighbouring registry entry; the current baseline registry order no
  longer matches that extraction assumption.
- Two `inquiry-intake-canary` assertions read the unchanged workflow file with
  Windows CRLF line endings while expecting LF/bash continuation text.

The isolated stay-network contract suite passed 7/7, and the combined
destination-Hub, page-family, owner-strengthening and public-content checks
passed 18/18. This report does not describe the full inquiry suite as green.

## Browser checks from the integration-split pass

Fifteen affected localized routes were loaded from the production static export
in the Codex in-app browser at a 320 CSS-pixel viewport. Every route rendered a
non-empty title, H1 and `main`; none returned an error page. The document client
width was 305 pixels after browser chrome/scrollbar allocation, and both root
and body scroll widths were 305 pixels on every route: horizontal overflow was
zero.

| Route | Result | Overflow |
| --- | --- | --- |
| `/destinations/hangzhou/` | EN title, H1 and main rendered | 0 px |
| `/zh/destinations/hangzhou/` | ZH title, H1 and main rendered | 0 px |
| `/ko/destinations/hangzhou/` | KO title, H1 and main rendered | 0 px |
| `/destinations/zhangjiajie/` | EN title, H1 and main rendered | 0 px |
| `/zh/destinations/zhangjiajie/` | ZH title, H1 and main rendered | 0 px |
| `/ko/destinations/zhangjiajie/` | KO title, H1 and main rendered | 0 px |
| `/guides/foreigners-china-hotel/` | EN title, H1 and main rendered | 0 px |
| `/zh/guides/foreigners-china-hotel/` | ZH title, H1 and main rendered | 0 px |
| `/ko/guides/foreigners-china-hotel/` | KO title, H1 and main rendered | 0 px |
| `/guides/china-accessible-hotel-room-verification/` | EN title, H1 and main rendered | 0 px |
| `/zh/guides/china-accessible-hotel-room-verification/` | ZH title, H1 and main rendered | 0 px |
| `/ko/guides/china-accessible-hotel-room-verification/` | KO title, H1 and main rendered | 0 px |
| `/guides/shanghai-where-to-stay-first-trip/` | EN title, H1 and main rendered | 0 px |
| `/zh/guides/shanghai-where-to-stay-first-trip/` | ZH title, H1 and main rendered | 0 px |
| `/ko/guides/shanghai-where-to-stay-first-trip/` | KO title, H1 and main rendered | 0 px |

## Date and lifecycle invariants

- Public copy prepared for this split uses `dateModified: 2026-08-21`.
- A page's `sourceReviewedDate` moves only where the source ledger records a
  complete factual recheck.
- Hangzhou and Zhangjiajie are published Hubs from PR #74; no file in this
  split may demote either route to pending or release-candidate status.
- All seven current Hub IDs must remain in `lib/destinationHubs.ts`.
- The four Search Map main-ledger files are owned by Employee 7 and are absent
  from this branch diff.

## Non-goals

This branch creates no public inventory, supplier connection, payment flow,
PR, merge or deployment. Missing image enhancements are recorded as rights or
commissioning gaps and are not filled with AI documentary scenes.
