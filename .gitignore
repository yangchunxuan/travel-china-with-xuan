node_modules/
.next/
out/
# Timestamped build-output backups. The `.next/` and `out/` rules above match
# only those exact directory names, so `.next.pre-*` and `.next.production-*`
# slipped through and left ~960 MB of webpack cache stageable by `git add .`.
.next.*/
out.*/
.DS_Store
npm-debug.log*
*.tsbuildinfo
__pycache__/
*.py[cod]
.env*
!.env.example
/supabase/.temp/
/路线介绍ui设计.mp4
DESIGN-BRIEF-*.md
.art-lab/
/tools/models/*.onnx
